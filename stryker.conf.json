{
  "$schema": "./node_modules/@stryker-mutator/core/schema/stryker-schema.json",
  "_": [
    "Root Stryker config. Scoped to the `extensions/` folder because that's",
    "the only contract surface currently covered by unit tests that run",
    "without Supabase (extensions.test.ts). The coachbyte/chefbyte MCP tool",
    "handlers in packages/app-tools/src/ have ONLY integration tests (live",
    "Supabase required) and the top-level vitest.config.ts excludes them —",
    "so vitest --related finds no tests for those files and Stryker errors",
    "with 'No tests were executed'. Follow-up: either add a unit-test layer",
    "for the coachbyte/chefbyte handlers or run stryker with a configured",
    "integration harness. See scripts/mutation/README.md."
  ],

  "packageManager": "pnpm",
  "testRunner": "vitest",
  "plugins": ["@stryker-mutator/vitest-runner"],

  "vitest": {
    "configFile": "packages/app-tools/vitest.config.ts",
    "dir": "packages/app-tools"
  },

  "coverageAnalysis": "perTest",
  "timeoutMS": 10000,
  "timeoutFactor": 1.5,
  "concurrency": 2,

  "mutate": [
    "extensions/**/*.ts",
    "!extensions/**/*.test.ts",
    "!extensions/**/*.spec.ts",
    "!extensions/**/__tests__/**"
  ],

  "ignorePatterns": [
    "dist",
    "coverage",
    "legacy",
    "hardware",
    "supabase/migrations",
    "packages/db-types/src/database.ts",
    "**/*.tsbuildinfo",
    ".verify",
    ".turbo",
    ".wrangler",
    ".vercel",
    ".stryker-tmp"
  ],

  "reporters": ["progress", "clear-text", "json"],
  "jsonReporter": { "fileName": "reports/mutation/mutation.json" },

  "thresholds": {
    "high": 60,
    "low": 40,
    "break": 40
  },

  "tempDirName": ".stryker-tmp",
  "cleanTempDir": true
}
