# Live Shelf server — merged Bundle H runtime dependencies.
#
# Install with:
#     python3 -m pip install -r requirements.txt
#
# Target: Python 3.13 on Raspberry Pi OS 64-bit. All packages below ship
# prebuilt aarch64 wheels.

# Core web + data
Flask>=3.0,<4
pydantic>=2.6,<3

# Camera / image processing
opencv-python-headless>=4.8,<5
numpy>=1.24,<3

# Classifier
anthropic>=0.40,<1

# Intake — OpenFoodFacts HTTP lookup
requests>=2.31,<3

# Deploy script (used from the dev host)
paramiko>=3.4,<4

# Tests
pytest>=8.0
hypothesis>=6
